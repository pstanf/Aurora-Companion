/* Optional local overrides — copy to config.local.js (gitignored).
   Use for staging sheet URLs, test phone numbers, etc. Do not put secrets here
   that belong in server-side env vars; this app is fully client-side. */
'use strict';

// Example:
// Object.assign(SITE, {
//   eventsSheetUrl: 'https://docs.google.com/spreadsheets/d/YOUR_SHEET/gviz/tq?tqx=out:csv&gid=0'
// });
